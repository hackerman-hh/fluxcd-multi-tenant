apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: tenant-a-prod

resources:
  - namespace.yaml
  - ../../base

patches:
  - target:
      kind: Kustomization
    patch: |
      - op: add
        path: /spec/path
        value: ./prod